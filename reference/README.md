# reference/ — Historical artifacts

Immutable historical material kept for archaeology, not for daily work.

| Directory | Contents |
|---|---|
| `git-bundles/` | Git bundles from the 2026-06 D2/L1D rebase operations (`d2-rebase-input.bundle`, `d2-rebased.bundle`, `l1d-rebased.bundle`). Kept as recovery artifacts for that rebase lineage. |

Vendored external repositories (e.g. `awesome-ai-agents*`) are untracked by
design (see `.gitignore`) and may live here on local checkouts.
