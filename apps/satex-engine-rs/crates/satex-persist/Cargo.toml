[package]
name = "satex-persist"
description = "Persistence: rusqlite (WAL) behind a dedicated-thread actor, VaultWriter (byte-compatible markdown), settings, learnings pruning, tracing-to-vault log bridge (Phase 4)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true
