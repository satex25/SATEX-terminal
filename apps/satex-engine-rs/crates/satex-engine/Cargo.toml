[package]
name = "satex-engine"
description = "TradingEngine orchestrator + ported pure cores (RS-2.x, Phase 2)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true
