[package]
name = "satex-risk"
description = "PERIMETER CRATE (human-gated, Appendix D): risk gates (15 display), funded profiles, kill-switch store (atomic write), live-mode arming, MAY-TACTICS (Phase 8)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true
