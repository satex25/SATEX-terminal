# scripts/archive/ — Retired one-off scripts

Historical, single-purpose scripts from past operations (PR #25 conflict
resolution, the D2/L1D rebase pushes, the 2026-06 root flattening, stale-file
cleanup). Kept for archaeology; none of these are part of any live workflow.

Live automation stays in `scripts/` proper (e.g. `update-baseline.sh`, which
is referenced by ARCHITECTURE.md and the nightly self-eval baseline process).
